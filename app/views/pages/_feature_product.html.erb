<%binding.pry%>
<% @product.each do |t|%>
	<% if t.feature==true%>
		<div class="turnover">
    	<div class="turnover_image">
      	<%= image_tag "#{t.image}"%>
      </div>
      <p>Brand Name:<%=t.name%></p>
    	<p>Model no:<%=t.model_no%></p>
      <input type="button" value="buy now" class="buy"/>
     </div>
	<%end%>
<%end%>

